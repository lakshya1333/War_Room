import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "WAR ROOM - AI-Powered Offensive Cyber Operations",
  description: "Advanced cybersecurity reconnaissance platform powered by Gemini 2.0 Flash Thinking. Real-time attack surface analysis, vulnerability detection, and automated exploit generation.",
  keywords: ["cybersecurity", "penetration testing", "vulnerability scanning", "AI security", "ethical hacking"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
